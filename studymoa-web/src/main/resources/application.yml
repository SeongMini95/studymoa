spring:
  profiles:
    include: db

  session:
    store-type: jdbc

  devtools:
    livereload:
      enabled: true

  thymeleaf:
    cache: false

logging:
  level:
    org.hibernate.type.descriptor.sql: trace

server:
  port: 8080